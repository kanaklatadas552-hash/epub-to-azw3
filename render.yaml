services:
  - type: web
    name: epub-to-azw3
    runtime: python
    buildCommand: "pip install -r requirements.txt && apt-get install -y calibre || true"
    startCommand: "uvicorn app:app --host 0.0.0.0 --port $PORT"
    plan: free
